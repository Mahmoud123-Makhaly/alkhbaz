'use server';

import React from 'react';

import { ReturnedOrdersList } from '@components';
import { notFound } from 'next/navigation';

const Page = () => {
  return notFound();
  // <ReturnedOrdersList />;
};

export default Page;
