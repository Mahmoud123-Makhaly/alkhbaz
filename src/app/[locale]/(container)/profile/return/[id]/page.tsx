'use server';

import React from 'react';
import { ReturnProcess } from '@components';

const Page = ({ params }: { params: { id: string } }) => {
  return <ReturnProcess />;
};

export default Page;
