'use server';

import React from 'react';
import { Wallet } from '@components';
import { notFound } from 'next/navigation';

const Page = () => {
  return notFound();
  // <Wallet />;
};

export default Page;
